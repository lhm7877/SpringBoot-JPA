spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop;MVCC=TRUE  #여러개 한번에 접근했을 때 더 빨리 처리가 됨, 권장
    username: sa
    password:
    driver-class-name: org.h2.Driver

 jpa:
   hibernate:
     ddl-auto: create
   properties:
     hibernate:
#       show_sql: true
       format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
